name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # Job 1 — Test & Typecheck
  # ─────────────────────────────────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test

  # ─────────────────────────────────────────────────────────────────────────────
  # Job 2 — Build
  # Validates the package actually builds and packs cleanly, and uploads dist/
  # so the release job (below) doesn't need to rebuild.
  # ─────────────────────────────────────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Verify build output
        run: |
          test -f dist/index.js
          test -f dist/index.cjs
          test -f dist/index.d.ts

      - name: Dry-run pack
        run: npm pack --dry-run

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 1

  # ─────────────────────────────────────────────────────────────────────────────
  # Job 3 — Release & Publish
  # Creates a GitHub Release and publishes to public npm on every push to main
  # when the version in package.json changes. Unlike liyaengine-core (which
  # publishes to GitHub Packages, a private-ish registry appropriate for an
  # internal engine dependency), this is a public developer-facing SDK, so it
  # publishes to registry.npmjs.org with a real NPM_TOKEN secret.
  # ─────────────────────────────────────────────────────────────────────────────
  release:
    name: Release & Publish
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    timeout-minutes: 10

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      # Read the version from package.json and check, independently, whether
      # (a) the release tag already exists and (b) this version is actually
      # on npm. These can disagree — e.g. the tag/release gets created, then
      # the publish step fails on auth — so a re-run must not treat "tag
      # exists" as "npm publish also happened" or a partial-failure release
      # becomes permanently stuck (the tag blocks re-creating the release,
      # while nothing actually retries the publish).
      - name: Check release + publish status
        id: version_check
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          echo "version=$PKG_VERSION" >> $GITHUB_OUTPUT

          if git ls-remote --tags origin "refs/tags/v$PKG_VERSION" | grep -q .; then
            echo "tag_exists=true" >> $GITHUB_OUTPUT
            echo "⏭️  v$PKG_VERSION tag already exists — skipping release creation"
          else
            echo "tag_exists=false" >> $GITHUB_OUTPUT
          fi

          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/@liyaengine/sdk/$PKG_VERSION")
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "published=true" >> $GITHUB_OUTPUT
            echo "⏭️  $PKG_VERSION already on npm — skipping publish"
          else
            echo "published=false" >> $GITHUB_OUTPUT
            echo "🚀 $PKG_VERSION not yet on npm — publishing"
          fi

      - name: Download dist artifact
        if: steps.version_check.outputs.tag_exists == 'false' || steps.version_check.outputs.published == 'false'
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Create GitHub Release
        if: steps.version_check.outputs.tag_exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version_check.outputs.version }}
          name: "@liyaengine/sdk v${{ steps.version_check.outputs.version }}"
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(steps.version_check.outputs.version, '-') }}

      # --ignore-scripts skips `prepublishOnly` (build) — dist/ is already
      # present from the artifact downloaded above.
      - name: Publish to npm
        if: steps.version_check.outputs.published == 'false'
        run: npm publish --ignore-scripts --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
